environment: homelab

image:
  # Updated by CI pipeline on every main-branch push via:
  # yq -i ".image.tag = \"${CI_COMMIT_SHORT_SHA}\"" environments/homelab.yaml
  tag: "placeholder"

# Single replica on the homelab cluster.
scaling:
  min: 1
