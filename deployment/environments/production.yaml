environment: production

image:
  # Updated by CI pipeline on every main-branch push via:
  # yq -i ".image.tag = \"${CI_COMMIT_SHORT_SHA}\"" environments/production.yaml
  tag: "placeholder"

# CPU HPA between 2 and 10 replicas.
scaling:
  min: 2
  max: 10
  targetCPUUtilization: 80
